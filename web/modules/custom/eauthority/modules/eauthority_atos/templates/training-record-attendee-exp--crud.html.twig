{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}
<div class="container-fluid">

    <div class="row">
        <div class="col-sm-2">{{ form.flight_hours }}</div>
        <div class="col-sm-4">{{ form.aircraft }}</div>
        <div class="col-sm-6">{{ form.flight_experience_types }}</div>
    </div>
    
    <div class="row">
        <div class="col-sm-12">{{ form.actions }}</div>
    </div>
    
    {{ form.form_build_id }}
    {{ form.form_token }}
    {{ form.form_id }}
</div>
