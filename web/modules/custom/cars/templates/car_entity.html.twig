{#
/**
 * @file car_entity.html.twig
 * Default theme implementation to present Car entity data.
 *
 * This template is used when viewing Car entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_car_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('car_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
